# PokéAPI is public and does not require an API key.
# Keep this file as a safe template for any future local-only configuration.
# Never commit a real .env file containing secrets.

# EXAMPLE_FUTURE_API_KEY=replace-me-locally
